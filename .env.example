# No environment variables required.
# LithoApp runs entirely in the browser — no API keys needed.
#
# If you extend the app with server-side features, add your keys here:
# GEMINI_API_KEY="your_key_here"
